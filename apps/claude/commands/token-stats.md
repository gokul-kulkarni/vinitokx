---
description: Show a compact token usage dashboard from local Claude Code transcripts (zero LLM cost).
argument-hint: "[--session <uuid>]"
---

Run the offline analyzer in compact mode and display its output verbatim.

!vinitokx analyze --compact $ARGUMENTS

Do not paraphrase the dashboard — show the CLI output directly.
